import SecurityPage from "@/pages/security";

export default function TenantSecurityPage() {
  // This component currently just inherits all functionality from the shared SecurityPage
  // In the future, we can add tenant-specific security features here if needed
  return <SecurityPage />;
}